async function typspeed() {
    let type = new Promise ((resolve, reject) => {
    //Timeout = 2 weeks (= 2000 ms)
    setTimeout(() => resolve(), 2000);
    });
    let 
};

function goal() {
    return new Promise ((resolve, reject) => {
        if
        else
    }
}

typspeed();

typspeed ()
    .then() {
        console.log("I reached my goal!")
    }
    .catch() {
        console.log("I failed :(")
    }
   